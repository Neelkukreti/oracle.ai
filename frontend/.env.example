# Required — point to your deployed backend
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws

# Optional — premium TTS voice (falls back to browser TTS if not set)
NEXT_PUBLIC_ELEVENLABS_API_KEY=your_elevenlabs_key_here
